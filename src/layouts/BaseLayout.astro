---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { withBase } from "../utils/url";
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Mélimo Services : ménage, garde d'enfants, jardinage, aide administrative, livraison de courses et bricolage, à Valenciennes et à Sarlat-la-Canéda.",
} = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <script is:inline>
      const stored = localStorage.getItem("theme");
      if (stored !== "light") {
        document.documentElement.classList.add("dark");
      }
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href={withBase("favicon.ico")} />
    <meta name="description" content={description} />
    <meta property="og:title" content={`${title} | Mélimo Services`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <title>{title} | Mélimo Services</title>
  </head>
  <body class="flex min-h-screen flex-col bg-bg text-text">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
